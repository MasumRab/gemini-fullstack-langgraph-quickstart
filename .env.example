# Gemini API Configuration
GEMINI_API_KEY=your_api_key_here

# Model Configuration (Optional - Overrides defaults in configuration.py)
# Optimized configuration: Flash-Lite for queries, Flash for reflection, Pro for answers
QUERY_GENERATOR_MODEL=gemini-1.5-flash
REFLECTION_MODEL=gemini-1.5-flash
ANSWER_MODEL=gemini-1.5-flash

# Research Configuration
NUMBER_OF_INITIAL_QUERIES=3
MAX_RESEARCH_LOOPS=2
REQUIRE_PLANNING_CONFIRMATION=true

# Optional: Tavily Search API (if using Tavily instead of Google Search)
# TAVILY_API_KEY=your_tavily_api_key_here
